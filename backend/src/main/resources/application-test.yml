spring:
  datasource:
    # TODO: Migrate to Testcontainers PostgreSQL auto-configuration
    url: jdbc:h2:mem:instagram_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  flyway:
    enabled: true
  jpa:
    show-sql: false
